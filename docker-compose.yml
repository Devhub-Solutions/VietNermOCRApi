version: '3.8'

services:
  ocr-api:
    build: .
    container_name: vietnerm-ocr-api
    ports:
      - "8000:8000"
    environment:
      - DB_PATH=/app/ocr_app.db
      - PYTHONUNBUFFERED=1
    restart: always
